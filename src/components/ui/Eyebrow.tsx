const Eyebrow = ({ children }: { children: React.ReactNode }) => (
  <p className="font-mono text-[11px] uppercase tracking-[0.2em] text-[#055449] mb-4">
    {children}
  </p>
);

export default Eyebrow;
